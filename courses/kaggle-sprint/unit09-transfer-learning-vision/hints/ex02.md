# ex02 ヒント

## Tier 1

ResNet18 の分類 head は `fc` です。C# で合成オブジェクトの末端部品だけ差し替えるように、新しい `nn.Linear` へ交換できます。

## Tier 2

入力特徴数は交換前の `model.fc.in_features` です。固定状態は各 `Parameter` の `requires_grad` で制御します。

## Tier 3

head の名前は `fc.`、最終 block の名前は `layer4.` から始まります。段階的解除ではこの2種類だけを True にします。
