# ex03 ヒント

## Tier 1

PyTorch の学習ループは「勾配を空にする→予測→loss→逆伝播→更新」です。混同行列は行が正解、列が予測です。

## Tier 2

分類 loss は `CrossEntropyLoss`、確率化はクラス軸の `softmax` です。precision の分母は列和、recall の分母は行和です。

## Tier 3

更新の中心は `loss.backward()` と `optimizer.step()` です。推論結果は `.cpu()` へ移してから連結します。分母が0の指標は0.0にします。
