# ex02_stacking_and_seed_average ヒント

## seed_average / correlation

### tier1
seedを新しい軸に積んで平均。モデル列は変数として相関を取る。

### tier2
`np.stack(...,axis=0).mean(axis=0)`、`np.corrcoef(matrix,rowvar=False)`。

### tier3
そのままfloat配列として返す。

## stacking

### tier1
OOFの各モデル列を普通の特徴量としてRidgeへ渡す。

### tier2
`Ridge(alpha=alpha).fit(oof_matrix,y_true)`。

### tier3
test側は同じ列順の行列をmodel.predictへ渡す。
